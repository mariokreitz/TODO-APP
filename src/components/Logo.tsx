/**
 * Renders the Logo component with an image of dots.
 *
 * @return {JSX.Element} The Logo component with the dots image.
 */
const Logo = (): JSX.Element => {
  return <img src="https://bytegrad.com/course-assets/react-nextjs/dots.png" />;
};

export default Logo;
